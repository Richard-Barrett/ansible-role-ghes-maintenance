# Role metadata

`main.yml` contains the Ansible Galaxy metadata for this role, including its
Galaxy name, minimum supported Ansible version, license, and platform metadata.

Keep the metadata synchronized with the compatibility and installation guidance
in the root `README.md` and `docs/README.md`.
