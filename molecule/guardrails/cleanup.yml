---
- name: Clean up guardrail test state
  hosts: all
  gather_facts: false
  tasks:
    - name: Remove the scenario test directory
      ansible.builtin.file:
        path: "/tmp/ansible-role-ghes-maintenance-guardrails"
        state: absent
