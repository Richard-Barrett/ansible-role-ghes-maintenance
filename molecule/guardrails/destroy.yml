---
- name: Destroy test instance
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Record that no delegated resources need destruction
      ansible.builtin.debug:
        msg: "The local Molecule instance has no external resources to destroy."
